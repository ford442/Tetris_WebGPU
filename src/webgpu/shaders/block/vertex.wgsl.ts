/**
 * Block vertex shader — shared across all production themes.
 * Source of truth: ../wgsl/block/vertex.wgsl (loaded raw, no transformation).
 */
import wgsl from '../wgsl/block/vertex.wgsl?raw';

export const BLOCK_VERTEX_SHADER_WGSL = wgsl;
