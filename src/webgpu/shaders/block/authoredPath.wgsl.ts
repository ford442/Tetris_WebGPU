/** Authored gold + crystal composition (IBL, backdrop refraction). Source of truth: ../wgsl/block/authoredPath.wgsl (loaded raw). */
import wgsl from '../wgsl/block/authoredPath.wgsl?raw';

export const BLOCK_AUTHORED_PATH_WGSL = wgsl;
