/**
 * Legacy block shader factories — not used at runtime.
 *
 * Production path: `../block/blockShader.ts` (`createBlockShaders`).
 * Underwater/premium visual differences are driven by CPU uniforms on the single pipeline.
 *
 * Kept for reference only; do not add new uniforms here.
 */
